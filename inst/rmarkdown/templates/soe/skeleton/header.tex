% Set up the fonts
\usepackage{fontspec}
\setmainfont{Garamond}
\newfontfamily\headingfont[]{Calibri}[Scale=1.4]
\renewcommand{\maketitlehooka}{\headingfont}

% Set the language for 508
\hypersetup{
  pdftitle = title,
  pdflang = en-US}

% Add accessibility support from http://www.richschwinn.com/accessibility
\RequirePackage{accsupp}
\RequirePackage{pdfcomment}
\newcommand{\AccTool}[2]{\BeginAccSupp{method=pdfstringdef,unicode,Alt={{#1}}}\pdftooltip{{#2}}{{#1}}\EndAccSupp{}}

% Set up the headers and footers
\usepackage{graphicx}
%\usepackage{stackengine} % For \stackunder command

% Add logo to the heading
\usepackage{titling}
\pretitle{\begin{flushleft}}
\posttitle{\hfill\includegraphics[width = 40pt]{NOAA_logo.png}\par\end{flushleft}}
