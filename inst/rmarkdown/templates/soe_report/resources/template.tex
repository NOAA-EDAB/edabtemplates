% General margins and paper-size
\documentclass[10pt,letterpaper]{article}
\usepackage[left=2cm, right=2cm, top=2cm, bottom=3cm, footskip = .5cm]{geometry}


% Set the language for 508
%\usepackage[pdflang={en-US}]{hyperref}

% Set the main and mono fonts
\usepackage{fontspec}
\setmainfont{Garamond}
\setmonofont{Calibri}

% Accessibility support
%\RequirePackage{accsupp}
%\RequirePackage{pdfcomment}
%\newcommand{\AccTool}[2]{\BeginAccSupp{method= pdfstringdef, unicode, Alt={{#1}}}\pdftooltip{{#2}}{{#1}}\EndAccSupp{}}


% Creates a more attractive heading
%\usepackage{fancyhdr}
%\usepackage{ifthen}
%\usepackage{everypage}

%\pagestyle{fancy}
%\fancyhf{}
%\setlength\headheight{40pt}
%\lhead{\texttt{\emph{State of the Ecosystem}} \\ \texttt{\emph{Gulf of Maine and Georges Bank}}}
%\cfoot{\thepage}

%\AddEverypageHook{%
 %  \ifthenelse{\value{page}=1}%
  %   {\rhead{\includegraphics[width=40pt]{NOAA_logo.png} \\ \texttt{\emph{Published March 3, 2018}}}
   %  }%
    % {\rhead{}
     %}
%}

%\renewcommand{\headrulewidth}{0.4pt}
%\renewcommand{\footrulewidth}{0pt}

\begin{document}


\end{document}
